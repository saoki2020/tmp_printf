#include <stdio.h>

int main(void)
{
	int acc1 = -1;
	int acc2 = -1;
	int acc3 = -1;
	int putlen = 0;
 int num = 0;
	int x;
	int i = 1;
	int j = 1;
	int n;


if ( 0 && 0)
 printf("OK\n");

	n = (j == 0 && i);

	printf("n=%d\n",n);

	acc1 = (acc1 <= putlen ? (num == 0 && acc1) : acc1 - putlen);

	if (acc2 <= putlen)
		acc2 = (num == 0 && acc2);
	else
		acc2 = acc2 - putlen;

	if (acc3 <= putlen)
	 {
			if (num == 0 && acc3)
				acc3 = 1;
			else
				acc3 = 0;
		}
	else
	 acc3 = acc3 - putlen;


	printf("%d\n",acc1);
	printf("%d\n",acc2);
	printf("%d\n",acc3);
	printf("%0d\n",num);

	x = printf("%d",0);
	printf("\n");
	printf("%d\n",x);

	}
